<!-- 预定区组件 -->
<view class="preorder-container">
  <view class="goods-grid">
    <block wx:for="{{goodsList}}" wx:key="id">
      <view class="goods-card">
        <view class="goods-image-wrapper">
          <image class="goods-image" src="{{item.image}}" mode="aspectFill"></image>
          <view class="goods-badge preorder">接龙预定</view>
        </view>
        <view class="goods-info">
          <text class="goods-name">{{item.name}}</text>
          <text class="goods-price">¥{{item.price}}</text>
          <text class="goods-stock">已预定{{item.preordered}}件</text>
          <text class="goods-spec">规格:{{item.spec}}</text>
        </view>
        <view class="goods-action" bindtap="onJoinGroup" data-item="{{item}}">
          参与接龙
        </view>
      </view>
    </block>
  </view>
  <view class="empty-state" wx:if="{{goodsList.length === 0}}">
    <text>暂无预定商品</text>
  </view>
</view>